    1.  Look for options around Enum arrays, and see about finishing up with that.
    2.  Probably proceed back to trait objects, and simply add in get struct methods and creation of subtype objects that have all the methods.  This will ally more with what we've
    read on recommendations.
